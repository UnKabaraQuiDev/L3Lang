let static int16 t = 2;
let static int16 fabrice = 2; // 18 M 4 = 2
let static int16 text = "Heloo world";

fun void exit(int8 code) {
	asm#("; Exit program");
	asm#("mov ebx, [esp + 4]");
	asm#("mov eax, 1 ; Syscall exit");
	asm#("int 0x80   ; Syscall call");
	// exit#(input);
}

//fun void printout(int16 text) {
//	asm#("; Prepare the arguments for the write system call");
//	asm#("mov eax, 4           ; System call number for write (syscall number)");
//	asm#("mov ebx, 1           ; File descriptor 1: stdout");
//	asm#("mov ecx, buffer      ; Pointer to the buffer to be printed");
//	asm#("mov edx, buffer_len  ; Length of the buffer in bytes");
//	asm#("int 0x80             ; Invoke the kernel");
//}

fun int main() {
	printout#(text);
	exit(69);
	return 12;
}